import React from "react";
import withMessage from "./withMessage";

const BillMessage2 = withMessage(() => (
  <div>
    Good times are just around the corner.
  </div>
));

export default BillMessage2;
